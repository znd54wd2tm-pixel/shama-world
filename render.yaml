services:
  - type: web
    name: shama-world
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    healthCheckPath: /api/health
    envVars:
      - key: BOT_TOKEN
        sync: false
